{
    "title": "Input schema for the apify_project actor.",
    "type": "object",
    "schemaVersion": 1,
    "properties": {
        "url": {
            "title": "URL to scrape",
            "type": "string",
            "description": "URL to scrape and extract HTML from",
            "editor": "textfield",
            "prefill": "https://apify.com"
        }
    }
}
